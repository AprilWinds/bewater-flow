# <需求名>

## 方案概览

> 让没参与过需求讨论的人 30 秒理解方案全貌。

## 目标

> 方案要解决什么问题。

## 核心设计

### 涉及已有模块

> 需要修改/新增的已有文件和改动类型。

### 接口/函数签名

> 精确签名只写入 tasks.json 各 task 的 `signatures`，design.md 不重复手写，避免双源分叉。此处只描述接口层设计意图：主要端点、关键调用关系、对外契约的稳定性约束。


### 数据模型

> 涉及的表/字段/类型。

### 关键流程

> 核心业务逻辑，文字或伪代码。

## 边界与约束

### 不处理的场景

> 每项一句话——「什么情况不处理，为什么」。

### 并发与数据保护

> 不涉及则写「本需求不涉及」。

### 安全考量

> 认证授权、注入防护、敏感数据泄露。

### 兼容性与回滚

> 数据库迁移是否可逆、API 向后兼容、配置降级方案。